export interface Track {
    artistId: string,
    artistName: string,
    albumId: string,
    albumName: string,
    trackName: string,
    trackNumber: number,
    discNumber: number,
    duration: number,
    sampleRate: number,
    fileExtension: string,
    recordingId: string,
    fileId: string,
    fileSize: number
}